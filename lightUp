public void lightUp(int x, int y, boolean horiz, boolean vert){
  for(int i=0;i<16;i++){
    if(horiz)
    {
        mainGUI.ibutton.buttons[y,i].setIcon(mainGUI.buttonOnUp);
    }
    if(vert)
    {
      mainGUI.ibutton.buttons[i,x].setIcon(mainGUI.buttonOnUp);
    }
  }
}
